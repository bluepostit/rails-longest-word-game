<div class="container m-4">
  <h1>Score</h1>
  <% if @error == :word_not_from_grid %>
    <p>Sorry, but <strong><%= @word.upcase %></strong>
       can't be built out of <%= @letters.join(', ').upcase %>.</p>
  <% elsif @error == :word_not_valid %>
    <p>Sorry, but <strong><%= @word.upcase %></strong>
      doesn't seem to be an English word.</p>
  <% else %>
    <p><strong>Congratulations!</strong> <%= @word.upcase %>
    is a valid English word!</p>
  <% end %>

  <%= link_to 'Play again', new_path %>

</div>
